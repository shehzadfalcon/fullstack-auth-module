export enum EUN_AUTH_ROUTES {
  LOGIN = "/login",
  SIGNUP = "/signup",
  FORGOT_PASSWORD = "/forgot-password",
  RESET_PASSWORD = "/reset-password",
  VERIFY_EMAIL = "/verify-email",
  VERIFY_OTP = "/otp-verify",
  LINK_ACCOUNT = "/link-account",
}

export enum EAUTH_ROUTES {
  HOME = "/",
}
